# Robot2 좌표 동기화 및 협업 용접 구현 계획

**작성일**: 2025-12-17
**프로젝트**: S25016 - 갠트리 듀얼 로봇 시스템
**목적**: Robot1과 Robot2의 협업 용접을 위한 좌표 동기화

---

## 🎯 목표

**최종 목적**: Robot1과 Robot2가 같은 작업물에 대해 협업 용접 수행

**핵심 과제**:
- Robot2는 갠트리와 기구적으로만 연결되어 있어 갠트리 이동 시 자신의 절대 위치를 모름
- Robot2가 World 좌표계에서 자신의 위치를 정확히 알아야 협업 가능

---

## 📐 시스템 구조

### 물리적 구성

```
                    Gantry (X, Y, Z, R)
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
   R-axis center      R-axis center      R-axis center
   (회전 중심)         (wobj0)           (고정점)
        │                  │                  │
        ├─ 488mm →    [Robot1 Base]    ← 488mm ─┤
        │                  │                  │
        │             Robot1 wobj0        Robot2
        │           (R축 중심)          (R축 중심 + 488mm)
        │                  │                  │
    [Robot2 Base]      [TCP1]             [TCP2]
```

### 좌표계 관계

1. **World 좌표계**: 고정된 절대 좌표계
2. **Gantry 좌표계**: World에 대해 Z축 기준 90도 회전
   - Gantry X → World Y
   - Gantry Y → World X
   - Gantry Z → World Z
3. **Robot1 wobj0**: Gantry R축 중심 (Robot1 Base - 488mm)
4. **Robot2 wobj0**: Robot2 Base (Gantry R축 중심 + 488mm, Robot1과 대칭)

### 제어 구성

- **컨트롤러**: 1대 (Multi-Move)
- **TASK1**: Robot1 제어
- **TASK2**: Robot2 제어
- **외부축**: Gantry (X1, Y, Z, R, X2) - TASK1에 연결

---

## 🔍 문제 분석

### Robot1의 경우 (정상)

```rapid
! Robot1은 갠트리 외부축에 연결되어 있음
current_pos := CJointT();  ! 로봇 관절 + 갠트리 외부축 모두 포함
tcp_world := CRobT(\Tool:=tool0);  ! World 좌표 자동 계산
```

✅ **장점**: ABB 컨트롤러가 자동으로 갠트리 이동을 고려하여 World 좌표 계산

### Robot2의 경우 (문제)

```rapid
! Robot2는 갠트리와 기구적으로만 연결 (제어 연결 없음)
current_pos := CJointT();  ! 로봇 관절만 포함, 갠트리 정보 ???
tcp_world := CRobT(\Tool:=tool0);  ! Robot2 Base 기준 로컬 좌표만 제공
```

❌ **문제**:
- 갠트리가 이동해도 Robot2는 자신의 절대 위치를 모름
- CRobT()는 Robot2 Base 기준 상대 좌표만 반환
- 협업 작업을 위해서는 World 절대 좌표 필요

---

## 🧪 1단계: 기능 확인 테스트 (현재 단계)

### Test 1: Robot2에서 갠트리 외부축 읽기 가능 여부

**파일**: `test_robot2_capabilities.mod` → TASK2에 로드

**테스트 코드**:
```rapid
PROC TestRobot2_ReadExternalAxes()
    VAR jointtarget current_joint;

    current_joint := CJointT();

    ! 갠트리 외부축이 포함되어 있는가?
    gantry_x := current_joint.extax.eax_a;
    gantry_y := current_joint.extax.eax_b;
    gantry_z := current_joint.extax.eax_c;
    gantry_r := current_joint.extax.eax_d;

    ! 결과를 /HOME/robot2_external_axes_test.txt에 저장
ENDPROC
```

**예상 결과**:
- **Case A**: 갠트리 외부축 값 읽기 성공 → 최선의 시나리오
- **Case B**: 외부축 값이 9E9 (undefined) → TASK1 공유 필요

### Test 2: TASK1 ↔ TASK2 PERS 변수 공유

**TASK1 파일**: `task1_shared_variables.mod`

```rapid
! TASK1에서 선언
TASK PERS jointtarget shared_gantry_pos;
TASK PERS num shared_gantry_x := 0;
TASK PERS num shared_gantry_y := 0;
TASK PERS num shared_gantry_z := 0;
TASK PERS num shared_gantry_r := 0;

PROC UpdateSharedGantryPosition()
    shared_gantry_pos := CJointT();
    shared_gantry_x := shared_gantry_pos.extax.eax_a;
    ! ...
ENDPROC
```

**TASK2에서 읽기**:
```rapid
! TASK2에서도 동일하게 선언 (TASK PERS)
TASK PERS num shared_gantry_x;

PROC ReadGantryFromTask1()
    gantry_x := shared_gantry_x;  ! TASK1이 업데이트한 값 읽기
ENDPROC
```

**예상 결과**:
- **성공**: Multi-Move 시스템에서 TASK PERS 공유 지원
- **실패**: 대안으로 파일 I/O 또는 시그널 사용

### Test 3: Robot2 TCP 로컬 좌표 확인

```rapid
PROC TestRobot2_TCPCoordinates()
    tcp_world := CRobT(\Tool:=tool0);
    tcp_wobj0 := CRobT(\Tool:=tool0\WObj:=wobj0);

    ! 결과를 /HOME/robot2_tcp_coordinates.txt에 저장
ENDPROC
```

**목적**: Robot2 Base 좌표계에서 TCP 위치 확인

---

## 🛠️ 2단계: 좌표 변환 구현 (테스트 후)

### 시나리오 A: Robot2가 외부축 직접 읽기 가능

```rapid
MODULE Robot2CoordinateSync
    PROC GetRobot2WorldPosition()
        VAR jointtarget current_pos;
        VAR robtarget tcp_local;
        VAR robtarget tcp_world;
        VAR num gx, gy, gz, gr;

        ! 1. 갠트리 위치 읽기
        current_pos := CJointT();
        gx := current_pos.extax.eax_a;
        gy := current_pos.extax.eax_b;
        gz := current_pos.extax.eax_c;
        gr := current_pos.extax.eax_d;

        ! 2. Robot2 로컬 TCP
        tcp_local := CRobT(\Tool:=tool0\WObj:=wobj0);

        ! 3. World 좌표 변환
        tcp_world := TransformRobot2ToWorld(tcp_local, gx, gy, gz, gr);

        RETURN tcp_world;
    ENDPROC
ENDMODULE
```

### 시나리오 B: TASK1으로부터 공유 변수 읽기

```rapid
MODULE Robot2CoordinateSync
    ! TASK1과 공유하는 변수
    TASK PERS num shared_gantry_x;
    TASK PERS num shared_gantry_y;
    TASK PERS num shared_gantry_z;
    TASK PERS num shared_gantry_r;

    PROC GetRobot2WorldPosition()
        VAR robtarget tcp_local;
        VAR robtarget tcp_world;

        ! 1. TASK1으로부터 갠트리 위치 읽기
        VAR num gx := shared_gantry_x;
        VAR num gy := shared_gantry_y;
        VAR num gz := shared_gantry_z;
        VAR num gr := shared_gantry_r;

        ! 2. Robot2 로컬 TCP
        tcp_local := CRobT(\Tool:=tool0\WObj:=wobj0);

        ! 3. World 좌표 변환
        tcp_world := TransformRobot2ToWorld(tcp_local, gx, gy, gz, gr);

        RETURN tcp_world;
    ENDPROC
ENDMODULE
```

### 좌표 변환 함수 (핵심)

```rapid
FUNC robtarget TransformRobot2ToWorld(robtarget robot2_tcp, num gx, num gy, num gz, num gr)
    VAR robtarget world_tcp;
    VAR pos gantry_offset;
    VAR pos robot2_offset;

    ! 1. 갠트리 위치 (Gantry 좌표계)
    gantry_offset.x := gx;
    gantry_offset.y := gy;
    gantry_offset.z := gz;

    ! 2. Robot2 Base = Gantry R축 중심 + 488mm (대칭)
    !    Robot1 Base = Gantry R축 중심 + 488mm (반대 방향)
    !    Robot2는 Robot1과 X축 기준 대칭
    robot2_offset.x := -488;  ! Robot1과 반대 방향
    robot2_offset.y := 0;
    robot2_offset.z := 0;

    ! 3. 좌표계 변환
    !    Gantry X → World Y
    !    Gantry Y → World X
    !    Gantry Z → World Z

    ! 갠트리 좌표 → World 좌표 (90도 회전)
    world_tcp.trans.x := gantry_offset.y + robot2_offset.x + robot2_tcp.trans.x;
    world_tcp.trans.y := gantry_offset.x + robot2_offset.y + robot2_tcp.trans.y;
    world_tcp.trans.z := gantry_offset.z + robot2_offset.z + robot2_tcp.trans.z;

    ! 4. R축 회전 적용 (추가 구현 필요)
    !    gr 각도만큼 회전 변환

    ! 5. 오리엔테이션 (쿼터니언 변환)
    world_tcp.rot := robot2_tcp.rot;  ! 일단 단순화

    RETURN world_tcp;
ENDFUNC
```

**주의**: 위 변환 수식은 **단순화된 버전**입니다. 실제로는:
- R축 회전 행렬 적용 필요
- 쿼터니언 곱셈으로 오리엔테이션 변환 필요
- 실제 테스트 데이터로 검증 및 보정 필요

---

## 🎯 3단계: 협업 용접 구현

### Work Object 동적 업데이트 방식 (권장)

```rapid
MODULE Robot2CollaborativeWelding
    ! 동적 work object
    PERS wobjdata wobj_dynamic;

    PROC InitializeDynamicWorkObject()
        ! 초기 설정
        wobj_dynamic.robhold := FALSE;
        wobj_dynamic.ufprog := TRUE;
        wobj_dynamic.ufmec := "";
        wobj_dynamic.uframe := [[0, 0, 0], [1, 0, 0, 0]];
        wobj_dynamic.oframe := [[0, 0, 0], [1, 0, 0, 0]];
    ENDPROC

    PROC UpdateDynamicWorkObject()
        VAR num gx, gy, gz, gr;
        VAR pos gantry_world;
        VAR orient gantry_rot;

        ! 1. 갠트리 위치 읽기 (TASK1 공유 변수 또는 직접 읽기)
        gx := shared_gantry_x;
        gy := shared_gantry_y;
        gz := shared_gantry_z;
        gr := shared_gantry_r;

        ! 2. Gantry 좌표 → World 좌표 변환
        gantry_world.x := gy;  ! Gantry Y → World X
        gantry_world.y := gx;  ! Gantry X → World Y
        gantry_world.z := gz;  ! Gantry Z → World Z

        ! 3. R축 회전
        gantry_rot := OrientZYX(gr, 0, 0);

        ! 4. wobj_dynamic 업데이트
        wobj_dynamic.uframe.trans := gantry_world;
        wobj_dynamic.uframe.rot := gantry_rot;
    ENDPROC

    PROC CollaborativeWeld()
        ! 갠트리 위치 동기화
        UpdateDynamicWorkObject();

        ! 이제 wobj_dynamic을 기준으로 작업
        MoveL target1, v100, z10, tool0\WObj:=wobj_dynamic;
        ArcL target2, v100, seam1, weld1, weave1, tool0\WObj:=wobj_dynamic;
        ArcL target3, v100, seam1, weld1, weave1, tool0\WObj:=wobj_dynamic;

        ! World 좌표 확인
        VAR robtarget world_tcp;
        world_tcp := CRobT(\Tool:=tool0\WObj:=wobj_dynamic);
    ENDPROC
ENDMODULE
```

**장점**:
- ✅ ABB 표준 기능 (work object) 활용
- ✅ 복잡한 좌표 변환 불필요
- ✅ 기존 용접 프로그램 재사용 가능

---

## 📋 테스트 시나리오

### Phase 1: 기능 확인 (현재)

1. **Robot2에서 `test_robot2_capabilities.mod` 로드**
2. **Test 1 실행**: `TestRobot2_ReadExternalAxes()`
   - 결과 확인: `/HOME/robot2_external_axes_test.txt`
3. **TASK1에 `task1_shared_variables.mod` 로드**
4. **TASK1에서 실행**: `UpdateSharedGantryPosition()`
5. **Test 2 실행**: TASK2에서 TASK1 변수 읽기 시도

### Phase 2: 좌표 변환 검증

1. **Robot1 갠트리 이동**:
   - X=0, Y=0, Z=0, R=0 (Home)
   - X=0.1m, Y=0, Z=0, R=0
   - X=0, Y=0.1m, Z=0, R=0
   - X=0, Y=0, Z=0.1m, R=0

2. **각 위치에서**:
   - Robot1 World 좌표 기록
   - Robot2 로컬 좌표 기록
   - Robot2 계산된 World 좌표 기록
   - 비교 및 오차 분석

3. **R축 회전 테스트**:
   - R=0°, 45°, 90°에서 좌표 검증

### Phase 3: 협업 용접 시뮬레이션

1. **간단한 직선 용접 경로**:
   - Robot1과 Robot2가 같은 시작점/끝점
   - 동기화 확인

2. **복잡한 3D 경로**:
   - 곡선, 회전 포함
   - 좌표 정확도 검증

---

## 🚀 실행 계획

### 즉시 실행 (오늘)

- [x] `test_robot2_capabilities.mod` 생성 완료
- [x] `task1_shared_variables.mod` 생성 완료
- [ ] **사용자 작업**: TASK2에 테스트 모듈 로드
- [ ] **사용자 작업**: Test 1 실행 및 결과 확인

### 다음 단계 (테스트 결과에 따라)

**Case A: Robot2가 외부축 직접 읽기 가능**
- [ ] 좌표 변환 함수 구현
- [ ] 검증 테스트

**Case B: TASK1 공유 변수 필요**
- [ ] TASK1 MainModule 수정 (공유 변수 추가)
- [ ] TASK2에서 읽기 테스트
- [ ] 좌표 변환 함수 구현

### 최종 단계

- [ ] 동적 work object 구현
- [ ] 협업 용접 프로시저 개발
- [ ] 실제 로봇 테스트
- [ ] 문서화 및 최적화

---

## ❓ 확인 필요 사항

### 물리적 구조

1. **Robot2 Base 위치**:
   - Gantry R축 중심으로부터 정확히 **488mm** 떨어져 있나요?
   - 방향은 Robot1과 **X축 기준 대칭**인가요?
   - 아니면 다른 방향인가요? (Y축? Z축?)

2. **Robot2 wobj0 정의**:
   - Robot2의 wobj0는 어디에 정의되어 있나요?
   - Robot2 Base와 같은 위치인가요?

### 컨트롤러 설정

3. **MOC.cfg 확인**:
   - Robot2는 외부축 설정이 어떻게 되어 있나요?
   - TASK2의 mechanical unit 이름은?

4. **Multi-Move 설정**:
   - Coordinated mode 사용 중인가요?
   - 아니면 Independent mode인가요?

---

## 📎 첨부 파일

1. `test_robot2_capabilities.mod` - Robot2 기능 확인 테스트
2. `task1_shared_variables.mod` - TASK1 공유 변수 정의

---

**다음 액션**:
1. TASK2에 `test_robot2_capabilities.mod` 로드
2. Test 실행 및 결과 보고
3. 결과에 따라 다음 단계 진행

**작성자**: Claude Code
**버전**: v1.0.0
